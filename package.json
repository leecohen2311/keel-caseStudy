{
  "name": "keel-case-study",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "bash scripts/test.sh",
    "migrate": "node scripts/migrate.ts"
  },
  "dependencies": {
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.0",
    "vitest": "^3.2.0"
  }
}
